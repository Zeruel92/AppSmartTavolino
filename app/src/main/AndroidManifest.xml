<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="iot.unisalento.it.appsmarttavolino" >

    <uses-permission android:name="android.permission.INTERNET" />
;
    <uses-permission android:name="android.permission.NFC" />
;
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Registrazione"
            android:label="@string/title_activity_registrazione" >
        </activity>
        <activity android:name=".MenuGenerale" >
        </activity>
        <activity android:name=".NoNfcLogin" >
        </activity>
        <activity android:name=".Preferenze" >
        </activity>
    </application>

</manifest>
